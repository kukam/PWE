<pre id='DATA_DUMPER'>
    [% PERL %]
        use Data::Dumper;
        my $dump = $stash;
        delete $dump->{'_PARENT'};
        delete $dump->{'_DEBUG'};
        my $dmp = Dumper($stash);
        $dmp =~ s/</&lt;/g;
        $dmp =~ s/>/&gt;/g;
        print $dmp;
    [% END %]
</pre>
